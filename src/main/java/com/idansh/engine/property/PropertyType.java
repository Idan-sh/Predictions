package com.idansh.engine.property;

/**
 * Enum class for the possible types of properties' values.
 * The possible types are: decimal, float, boolean, string
 */
public enum PropertyType {
    DECIMAL, FLOAT, BOOLEAN, STRING;
}
